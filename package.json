{
	"scripts": {
		"start": "node index.js"
		, "dev": "cross-env NODE_ENV=development node index.js"

		, "deploy": "git push --force heroku master"
	}
	, "dependencies": {
		"nightmare": "^3.0.2"
	}
	, "devDependencies": {
		"cross-env": "5.x"
	}
	
}